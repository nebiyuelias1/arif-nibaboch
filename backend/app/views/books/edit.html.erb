<div class="max-w-2xl mx-auto bg-surface rounded-lg shadow p-6">
	<h1 class="text-2xl font-bold mb-4 text-content">Edit Book</h1>

	<%= render 'form', book: @book %>

	<div class="mt-4">
		<%= link_to 'Back', books_path, class: 'text-sm text-primary hover:underline' %>
	</div>
</div>
