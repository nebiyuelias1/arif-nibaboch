<div class="max-w-4xl mx-auto mt-8">
  <nav class="flex mb-6" aria-label="Breadcrumb">
    <ol class="inline-flex items-center space-x-1 md:space-x-3">
      <li class="inline-flex items-center">
        <%= link_to "Book Clubs", book_clubs_path, class: "text-sm font-medium text-content-subtle hover:text-accent" %>
      </li>
      <li>
        <div class="flex items-center">
          <svg class="w-3 h-3 text-content-muted mx-1" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 9 4-4-4-4"/>
          </svg>
          <%= link_to @book_club.name, book_club_path(@book_club), class: "text-sm font-medium text-content-subtle hover:text-accent" %>
        </div>
      </li>
      <li aria-current="page">
        <div class="flex items-center">
          <svg class="w-3 h-3 text-content-muted mx-1" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 9 4-4-4-4"/>
          </svg>
          <span class="text-sm font-medium text-content-subtle"><%= @book_read.book.title %></span>
        </div>
      </li>
    </ol>
  </nav>

  <div class="bg-surface p-8 rounded-lg shadow-sm border border-border mb-8">
    <div class="flex flex-col md:flex-row gap-8">
      <div class="w-full md:w-1/3 shrink-0">
        <% if @book_read.book.cover_image.present? %>
          <%= image_tag @book_read.book.cover_image, class: "w-full rounded-lg shadow-md aspect-auto" %>
        <% else %>
          <div class="w-full aspect-[2/3] bg-background rounded-lg flex items-center justify-center shadow-inner border border-border">
            <span class="text-content-muted text-sm font-medium">No cover available</span>
          </div>
        <% end %>
      </div>

      <div class="w-full md:w-2/3 flex flex-col">
        <div class="flex justify-between items-start mb-2">
          <h1 class="text-3xl font-bold text-content leading-tight"><%= @book_read.book.title %></h1>
          <span class="px-3 py-1 rounded-full text-sm font-semibold whitespace-nowrap ml-4
            <%= @book_read.status == 'completed' ? 'bg-background text-primary' :
                @book_read.status == 'active' ? 'bg-background text-primary' :
                'bg-background text-content' %>">
            <%= @book_read.status.titleize %>
          </span>
        </div>
        
        <p class="text-xl text-content-subtle mb-6">By <%= @book_read.book.author %></p>

        <div class="bg-background rounded-xl p-6 mb-8 border border-border">
          <h3 class="text-xs font-semibold text-primary uppercase tracking-wider mb-4">Reading Session Details</h3>
          <div class="grid grid-cols-2 gap-6">
            <div>
              <p class="text-sm text-content-muted mb-1">Start Date</p>
              <p class="font-semibold text-content"><%= @book_read.start_date&.strftime("%B %d, %Y") || 'Not set' %></p>
            </div>
            <div>
              <p class="text-sm text-content-muted mb-1">End Date</p>
              <p class="font-semibold text-content"><%= @book_read.end_date&.strftime("%B %d, %Y") || 'Not set' %></p>
            </div>
          </div>
        </div>

        <% if @book_read.book.description.present? %>
          <div class="mb-8">
            <h3 class="text-lg font-bold text-content mb-3">About the Book</h3>
            <p class="text-content-subtle leading-relaxed whitespace-pre-line"><%= @book_read.book.description %></p>
          </div>
        <% end %>

        <div class="mt-auto flex flex-wrap gap-4">
          <%= link_to "View Book Details", book_path(@book_read.book), class: "inline-flex items-center px-4 py-2 border border-border shadow-sm text-sm font-medium rounded-md text-content bg-surface hover:bg-background focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-colors" %>
          <%= link_to "Back to Club", book_club_path(@book_club), class: "inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-primary-contrast bg-primary hover:bg-primary focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-colors" %>
          <% if user_signed_in? && @book_club.owner == current_user %>
            <%= link_to "Edit Session", edit_book_club_book_read_path(@book_club, @book_read), class: "inline-flex items-center px-4 py-2 border border-border shadow-sm text-sm font-medium rounded-md text-content bg-surface hover:bg-background focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-colors ml-auto" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <!-- Discussion Questions Section -->
  <div class="bg-surface p-8 rounded-lg shadow-sm border border-border">
    <h2 class="text-2xl font-bold text-content">Discussion Questions</h2>
    
    <% if user_signed_in? && @book_club.owner == current_user %>
      <div class="mb-6 flex flex-col">
        <%= render partial: "discussion_questions/form", locals: { book_club: @book_club, book_read: @book_read, discussion_question: DiscussionQuestion.new } %>
      </div>
    <% else %>
      <%= turbo_stream_from @book_read, :discussion_questions %>
    <% end %>

    <div id="discussion_questions_list" class="space-y-5">
      <% @discussion_questions.where.not(id: nil).each do |question| %>
        <%= render partial: "discussion_questions/discussion_question", locals: { discussion_question: question } %>
      <% end %>
    </div>

    <div id="no_discussion_questions" class="text-center py-10 bg-background rounded-lg border border-dashed border-border mt-4 <%= 'hidden' if @discussion_questions.any? %>">
      <svg class="mx-auto h-12 w-12 text-content-muted" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
      </svg>
      <h3 class="mt-2 text-sm font-medium text-content">No discussion so far</h3>
      <p class="mt-1 text-sm text-content-subtle">Check back later for questions to discuss!</p>
    </div>
  </div>
</div>
